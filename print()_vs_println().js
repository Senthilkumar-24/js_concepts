/**
 * The println("...") method prints the string "..." and moves the cursor to a new line. 
 * The print("...") method instead prints just the string "...", but does not move the cursor to a new line.
 */
/**
 * In Javascript print method to write the output. instead it pops up the print window to print the current screen.
 * The print method is available inside the window object.
 */
